/*
 * pojavexec_stub.c
 * Empty stub library. libpojavexec.so depends on libpgw.so,
 * which provides all pojavInit/pojavCreateContext/etc. symbols.
 * This exists because LWJGL 3's GLFW class calls System.loadLibrary("pojavexec").
 */
